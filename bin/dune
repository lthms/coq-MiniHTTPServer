(coq.extraction
  (prelude Extract)
  (theories MiniHTTPServer)
  (extracted_modules minihttpserver))

(executable
  (name minihttpserver)
  (flags -open Coqbase)
  (public_name minihttpserver)
  (package coq-minihttpserver)
  (libraries unix coq-base.lib coq-minihttpserver.lib))